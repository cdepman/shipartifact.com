name = "pushtostart-sites"
main = "src/index.ts"
compatibility_date = "2024-12-01"

routes = [
  { pattern = "*.pushtostart.ai/*", zone_name = "pushtostart.ai" }
]

[[r2_buckets]]
binding = "SITE_BUCKET"
bucket_name = "pushtostart-sites"

[[unsafe.bindings]]
name = "AI_RATE_LIMITER"
type = "ratelimit"
namespace_id = "0"
simple = { limit = 10, period = 60 }
